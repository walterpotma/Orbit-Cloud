/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dnsRemoveRecord } from "../../funcs/dnsRemoveRecord.js";
import { RemoveRecordRequest$inboundSchema } from "../../models/removerecordop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveRecordRequest$inboundSchema,
};

export const tool$dnsRemoveRecord: ToolDefinition<typeof args> = {
  name: "dns-remove-record",
  description: `Delete a DNS record

Removes an existing DNS record from a domain name.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dnsRemoveRecord(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
