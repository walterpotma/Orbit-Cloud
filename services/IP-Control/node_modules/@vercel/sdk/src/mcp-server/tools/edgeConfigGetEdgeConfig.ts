/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { edgeConfigGetEdgeConfig } from "../../funcs/edgeConfigGetEdgeConfig.js";
import { GetEdgeConfigRequest$inboundSchema } from "../../models/getedgeconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEdgeConfigRequest$inboundSchema,
};

export const tool$edgeConfigGetEdgeConfig: ToolDefinition<typeof args> = {
  name: "edge-config-get-edge-config",
  description: `Get an Edge Config

Returns an Edge Config.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await edgeConfigGetEdgeConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
