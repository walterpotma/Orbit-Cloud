/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsJoinTeam } from "../../funcs/teamsJoinTeam.js";
import { JoinTeamRequest$inboundSchema } from "../../models/jointeamop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: JoinTeamRequest$inboundSchema,
};

export const tool$teamsJoinTeam: ToolDefinition<typeof args> = {
  name: "teams-join-team",
  description: `Join a team

Join a team with a provided invite code or team ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsJoinTeam(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
