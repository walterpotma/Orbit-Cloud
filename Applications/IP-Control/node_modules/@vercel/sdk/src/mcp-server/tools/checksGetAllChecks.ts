/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksGetAllChecks } from "../../funcs/checksGetAllChecks.js";
import { GetAllChecksRequest$inboundSchema } from "../../models/getallchecksop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAllChecksRequest$inboundSchema,
};

export const tool$checksGetAllChecks: ToolDefinition<typeof args> = {
  name: "checks-get-all-checks",
  description: `Retrieve a list of all checks

List all of the checks created for a deployment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksGetAllChecks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
