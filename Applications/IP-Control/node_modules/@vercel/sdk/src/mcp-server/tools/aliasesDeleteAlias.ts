/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aliasesDeleteAlias } from "../../funcs/aliasesDeleteAlias.js";
import { DeleteAliasRequest$inboundSchema } from "../../models/deletealiasop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAliasRequest$inboundSchema,
};

export const tool$aliasesDeleteAlias: ToolDefinition<typeof args> = {
  name: "aliases-delete-alias",
  description: `Delete an Alias

Delete an Alias with the specified ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aliasesDeleteAlias(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
