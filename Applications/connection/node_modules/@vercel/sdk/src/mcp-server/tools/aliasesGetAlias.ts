/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aliasesGetAlias } from "../../funcs/aliasesGetAlias.js";
import { GetAliasRequest$inboundSchema } from "../../models/getaliasop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAliasRequest$inboundSchema,
};

export const tool$aliasesGetAlias: ToolDefinition<typeof args> = {
  name: "aliases-get-alias",
  description: `Get an Alias

Retrieves an Alias for the given host name or alias ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aliasesGetAlias(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
