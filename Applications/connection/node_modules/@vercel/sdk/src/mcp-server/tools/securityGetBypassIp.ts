/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { securityGetBypassIp } from "../../funcs/securityGetBypassIp.js";
import { GetBypassIpRequest$inboundSchema } from "../../models/getbypassipop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetBypassIpRequest$inboundSchema,
};

export const tool$securityGetBypassIp: ToolDefinition<typeof args> = {
  name: "security-get-bypass-ip",
  description: `Read System Bypass

Retrieve the system bypass rules configured for the specified project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await securityGetBypassIp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
