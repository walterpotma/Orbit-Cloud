/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsDeleteProject } from "../../funcs/projectsDeleteProject.js";
import { DeleteProjectRequest$inboundSchema } from "../../models/deleteprojectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteProjectRequest$inboundSchema,
};

export const tool$projectsDeleteProject: ToolDefinition<typeof args> = {
  name: "projects-delete-project",
  description: `Delete a Project

Delete a specific project by passing either the project \`id\` or \`name\` in the URL.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsDeleteProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
