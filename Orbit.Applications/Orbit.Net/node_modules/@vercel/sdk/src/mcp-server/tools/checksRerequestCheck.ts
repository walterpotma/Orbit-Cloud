/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksRerequestCheck } from "../../funcs/checksRerequestCheck.js";
import { RerequestCheckRequest$inboundSchema } from "../../models/rerequestcheckop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RerequestCheckRequest$inboundSchema,
};

export const tool$checksRerequestCheck: ToolDefinition<typeof args> = {
  name: "checks-rerequest-check",
  description: `Rerequest a check

Rerequest a selected check that has failed.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksRerequestCheck(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
