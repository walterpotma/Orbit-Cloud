/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetWebhooks } from "../../funcs/webhooksGetWebhooks.js";
import { GetWebhooksRequest$inboundSchema } from "../../models/getwebhooksop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWebhooksRequest$inboundSchema,
};

export const tool$webhooksGetWebhooks: ToolDefinition<typeof args> = {
  name: "webhooks-get-webhooks",
  description: `Get a list of webhooks

Get a list of webhooks`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksGetWebhooks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
